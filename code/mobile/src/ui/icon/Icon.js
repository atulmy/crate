// Imports
import React from 'react'
import { MaterialIcons } from '@expo/vector-icons';

// Component
const Icon = (props) => {
  return(
    <MaterialIcons
      {...props}
    />
  )
}

export default Icon
